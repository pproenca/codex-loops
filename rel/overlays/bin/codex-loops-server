#!/bin/sh
set -eu

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
release_bin="$script_dir/agent_loops"

case "${1:-}" in
  foreground)
    shift
    exec "$release_bin" start "$@"
    ;;
  *)
    echo "Usage: codex-loops-server foreground" >&2
    exit 2
    ;;
esac
