#!/bin/sh
set -eu

bin_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
release_bin="$bin_dir/agent_loops"

argv_b64=$(
  for arg do
    printf '%s\0' "$arg"
  done | base64 | tr -d '\n'
)

export AGENT_LOOPS_ARGV_B64="$argv_b64"
exec "$release_bin" eval "Workflow.ReleaseCLI.main_from_env()"
